

@{
    Layout = null;

}


@*@Html.CheckBoxList("chkModelsOrigem", (ViewBag.origens as MultiSelectList), Position.Vertical, new { @class = "form-control" }, 1)*@

@foreach (var item in ViewBag.origens)
{
    var check = (((List<NovoControleProjetos.Models.Checkados>)ViewBag.Checkadas).Any(x => x.id_checkado == item.Id_Origem));

    <div class="form-inline">

        <input id="origensCheck" type="checkbox" name="check-origem" value="@item.Id_Origem" checked="@check" />
        <font size= "2"> @item.Ds_Origem </font>
        @*@if (((List<NovoControleProjetos.Models.Checkados>)ViewBag.Checkadas).Any(x => x.id_checkado == item.Id_Origem))

            //@if (ViewBag.Checkadas.Contains(item.Id_Origem))
            {
                <input id="origensCheck" type="checkbox" name="check-origem" value="@item.Id_Origem" checked="checked" />
                <label> @item.Ds_Origem </label>
            }
            else
            {
                <input id="origensCheck" type="checkbox" name="check-origem" value="@item.Id_Origem" />
                <label> @item.Ds_Origem </label>
            }*@

    </div>
    }
